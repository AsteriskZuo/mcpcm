# 项目介绍

当前项目做了比较大的改造。

该项目原来的所有内容都移动到了 app 文件夹。

从 mcpcm-server 迁移过来的内容 放在 api 文件夹。

## app

使用 `mcpcm add` 将收集的数据上传到服务器，使用 `mcpcm find` 将查找 mcp 信息。（// todo: 需要完成这两个改造）

app 目录下有原来的 AGENTS.md

## api

提供数据收集和查询的能力。

api 目录下有原来的 AGENTS.md
